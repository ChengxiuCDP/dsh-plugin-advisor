# SKILL: dsh-plugin-advisor（插件发现）

## 何时使用

用户想找、推荐、发现或安装 DeepSeek Harness 插件，或用自然语言描述一个能力需求（读图、终端界面、手机访问、余额管理、审批等）时，使用 `find_dsh_plugins` 工具。

## 用法

1. 把用户需求原样作为 `query` 传入（不要自己改写）；必要时可加 `limit`（默认 5）或 `installable_only: true`（只推荐可安装插件）。
2. 结果按「功能位匹配 + 质量分 + 星标」排序，每条含 `why`（功能位/质量分/可安装声明/风险）与 `install_cmd`。
3. 展示 Top 2-3 条：每条一句话说明为什么匹配、引用质量分与风险徽章；`warn` 非空（NOASSERTION/无许可证/停更/未收录）时必须提示用户。
4. 默认用中文回复（除非用户用英文）。
5. 给出安装命令，但**不要主动执行安装**；除非用户明确要求，否则先征得同意。

## 规则

- 推荐 ≠ 安全审计：安装插件会在本机运行第三方代码。低质量分（<60）或带风险徽章的插件，明确告知风险。
- 若前几名都不合适，说明「已搜索全量 N 个仓库，没有更好的匹配」，不要编造插件。
- 数据快照有日期（`data_date`），`live: true` 表示叠加了实时 GitHub 搜索。
